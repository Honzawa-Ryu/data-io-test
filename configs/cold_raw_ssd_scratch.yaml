# cold計測 1セル(raw x ssd_scratch)。投入直前に手動drop必須(docs/howto.md)。
purpose: campaign
repetitions: 1                    # external では1固定

cache_policy:
  mode: cold
  drop_caches_strategy: external

matrix:
  format: [raw]
  storage: [ssd_scratch]
  num_workers: [8]
  shuffle_mode: [shard]
  decode: [true]

dataset:
  ref: synth_100k_224_v1


output:
  jsonl: results/trials_cold.jsonl
  purpose_tag: campaign
