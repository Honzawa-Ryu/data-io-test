# Slurm クラスタ ストレージ/IO ベンチマーク実施指示書

## 0. ミッション

Slurm クラスタ上の機械学習(画像)ワークロードについて、実測に基づいて以下の 3 つの意思決定を行うための計測・分析・レポート作成を実施せよ。

1. **供給経路の判断**: どの条件で HDD / NFS から直接プロセスにデータ供給してよく、どの条件で SSD scratch へのステージングを挟むべきか(損益分岐条件の定式化)
2. **フォーマットの判断**: 生ファイル / WebDataset / HDF5 / Zarr のうち、どのケースでどれを推奨するか。あわせてシャード・チャンクの最適サイズ帯を特定する
3. **ステージング運用の判断**: ステージングを「ジョブ内(A)」「依存ジョブによる事前転送(B)」「ノードローカル共有キャッシュ(C)」のどれで運用すべきか

最終成果物は「推奨事項と根拠データをまとめたレポート」+「再実行可能なベンチマークスクリプト一式」である。数値を出すことではなく、**運用ルールとして書ける結論を出すこと**がゴール。

## 1. 環境情報

- Slurm クラスタ。Parent(ヘッド)ノード 1 台 + 計算ノード複数台
- 計算ノードは 3 構成:
  - **構成 X**: Parent と NFS 共有のファイルサーバをマウント + ノードローカル NVMe SSD scratch
  - **構成 Y**: ノードローカル HDD 記憶領域 + SSD scratch
  - **構成 Z**: GB10、ユニファイドメモリ(CPU/GPU 共有メモリ)
- マスターデータの置き場所は NFS ファイルサーバとする

### 実行前に人間に確認すること(不明なら質問せよ。推測で進めない)

- [ ] 各構成のノード名 / パーティション名 / constraint
- [ ] 各ストレージの実マウントパス(NFS、HDD、SSD scratch、`$SLURM_TMPDIR` の有無)
- [ ] 各ストレージの空き容量(ベンチデータを置けるか)
- [ ] `drop_caches` を実行できるか(root / sudo / prolog のいずれか)。不可の場合は「RAM の 2 倍以上のデータセット」で代替する
- [ ] `--exclusive` でノードを取ってよいか、計測に使ってよい時間帯
- [ ] 実ワークロードの想定: 画像 1 枚のサイズ・形式、データセット総量、モデル種別、典型的なエポック数、同時実行ジョブ数
- [ ] fio, PyTorch, webdataset, h5py, zarr(v3 対応版), tensorstore(任意)のインストール可否

## 2. 全体ルール(すべての計測に適用)

1. **ページキャッシュ排除**: 各試行前に `sync; echo 3 > /proc/sys/vm/drop_caches` を実行。不可能な場合は RAM より十分大きいデータで計測し、その旨をレポートに明記
2. **反復**: 各条件 3 回以上実行し中央値を採用。ばらつき(min/max)も記録
3. **占有**: 計測ジョブは `--exclusive` で投入(多重負荷試験を除く)
4. **副計測の常時取得**: すべての計測中、`iostat -x 1`、`dstat`(なければ `vmstat 1` + `sar`)、GPU 使用時は `nvidia-smi dmon -s u -d 1` をログに保存。「どのリソースが飽和したか」を後から特定できるようにする
5. **記録形式**: すべての結果は CSV(条件列 + 指標列)に集約。1 行 = 1 試行。集計・グラフはこの CSV から生成する
6. **再現性**: すべてのスクリプトは引数でパス・条件を切り替え可能にし、Git 管理する。ジョブスクリプトのテンプレート化を行う

## 3. 計測指標(優先順)

| 指標 | 定義 | 取得方法 |
|---|---|---|
| 実効スループット | DataLoader がデコード込みで供給できる samples/s と MB/s | 計測用ループで time 計測 |
| GPU 待ち時間 | データ待ちによる GPU アイドル | PyTorch Profiler / dmon の使用率 |
| ステージング時間 T_stage | ソース→SSD 転送の所要時間 | time |
| first-batch latency | ジョブ開始→最初のバッチ取得まで | time |
| 多重負荷劣化率 | N ジョブ同時実行時の単独比スループット | 同時投入して各ジョブで計測 |
| メタデータ性能 | NFS 上の open/stat ops/s | fio / 自作スクリプト |

## 4. フェーズ計画

**フェーズ 1 → 2 → 3 は必ずこの順で実施。** フェーズ 3 の結果を人間に報告し、方針確認を得てからフェーズ 4 以降に進むこと。

### フェーズ 1: ストレージ素性の把握(fio)

対象: NFS マウント、HDD(構成 Y)、NVMe scratch(構成 X, Y)、GB10 のローカルストレージおよび tmpfs。

- シーケンシャルリード / ランダムリード × ブロックサイズ {4K, 128K, 1M, 16M}
- iodepth {1, 32} × numjobs {1, 8}
- NFS のみ追加: 小ファイル大量 open/stat 試験(例: 4KB ファイル 10 万個の stat + read)
- 出力: ストレージごとの上限性能表。以降のフェーズの解釈基準とする

### フェーズ 2: データセット生成

- 合成画像データセットを生成(実ワークロードのヒアリング結果に寄せる。デフォルト: JPEG 約 100–150KB/枚 × 約 70 万枚 ≒ 100GB)
- 同一内容を 4 形態で用意:
  1. 生ファイル(1 ディレクトリあたりのファイル数も変数: 1k / 10k / 100k)
  2. WebDataset tar シャード(シャードサイズ: 10MB / 100MB / 500MB / 1GB / 5GB の 5 系列)
  3. HDF5(単一ファイル、チャンク: 64KB / 1MB / 16MB)
  4. Zarr v3 sharded(同チャンク掃引)+ 参考として Zarr v2(チャンク=1 ファイル)を小規模で
- 生成後、各形態の「ファイル数・合計サイズ・生成時間」を記録(保管コストの比較材料)

### フェーズ 3: フォーマット × ストレージ × アクセスパターン(本命)

PyTorch DataLoader で 1 エポック読み切る時間を計測。

- 配置: NFS / HDD / SSD(構成 X, Y で該当するもの)、GB10 は tmpfs も
- フォーマット: フェーズ 2 の 4 形態(シャード/チャンクサイズ掃引込み)
- num_workers: {0, 4, 8, 16}
- シャッフル: なし / シャード内シャッフル / 完全ランダム
- デコードなし(バイト読みのみ) vs デコード込み の 2 系列(IO ボトルネックと CPU ボトルネックの切り分け)
- 全組み合わせは膨大なので、まず {WebDataset 500MB, 生ファイル} × {NFS, HDD, SSD} × {workers 8} × {シャードシャッフル} × {デコード込み} をスモークとして先行実施し、傾向を見てから掃引範囲を絞ってよい。絞った場合は理由を記録

**→ ここで中間報告。フェーズ 3 の結果サマリを提示し、続行方針の確認を得ること。**

### フェーズ 4: ステージング損益分岐

- 転送計測: NFS→SSD、HDD→SSD を {cp -r, rsync, tar パイプ, シャード単位並列 rsync} で比較。シャードサイズとの交互作用を見る
- 損益分岐の算出: `T_stage < E × (T_epoch_direct − T_epoch_SSD)` を満たす最小エポック数 E を、構成 × フォーマットごとに表にする
- rsync `--bwlimit` あり/なしでの転送時間も記録(NFS 保護ポリシー検討用)

### フェーズ 5: 多重負荷

- NFS 直読みジョブを 2 / 4 / 8 本同時投入(複数ノードから)し、単独時比の劣化率を計測
- 同時に「1 本だけ SSD ステージング方式」を混ぜた場合の当該ジョブの安定性も確認
- 転送ジョブ(フェーズ 4 の方式)が並走した際の学習ジョブへの干渉も 1 条件計測

### フェーズ 6: ステージング運用パターンの試作

- **パターン A(ジョブ内)**: ジョブスクリプト冒頭でステージング。ベースライン
- **パターン B(依存ジョブ)**: CPU パーティションの転送ジョブ + `--dependency=afterok` + `-w` ノード固定の学習ジョブ。動作するテンプレートを作成し、GPU 遊休削減量を A と比較
- **パターン C(共有キャッシュ)**: `/scratch/cache/<dataset_hash>/` 方式。flock による排他、存在チェックによるスキップ、容量管理スクリプト(LRU 削除)まで実装。2 本目のジョブでステージングがスキップされることを実証
- GB10 は tmpfs ステージング(パターン A 相当)で十分かを確認

### フェーズ 7: (余力があれば)書き込み側

- チェックポイント書き込み: NFS 直書き vs ローカル SSD 書き→非同期 NFS 退避 の学習ブロッキング時間比較

## 5. 検証すべき仮説(結果はこれに対する判定として整理する)

- H1: HDD 直読みは「シーケンシャル形式 + シャードシャッフルのみ + 単独ジョブ + 要求スループット ≤ HDD 逐次読み性能」で成立する
- H2: 完全ランダムアクセスは HDD / NFS 上では実用不可レベルに劣化する
- H3: WebDataset のシャード最適帯は 100MB–1GB。HDD 直読みでは大きめが有利
- H4: 生ファイルは SSD 上では十分速いが NFS 上では最悪
- H5: NFS の多重負荷劣化は直読み方式で顕著、ステージング方式はほぼ免疫
- H6: GB10 ではデータがメモリに収まる場合フォーマット差がほぼ消え、CPU デコードが支配的になる
- H7: 同一データセットを繰り返し使う運用ではパターン C が総所要時間で最良

各仮説について「支持 / 棄却 / 条件付き支持」と根拠データを明記すること。

## 6. 最終成果物

1. `report.md`: 意思決定 3 点への回答、仮説判定表、損益分岐表(構成 × フォーマット × エポック数閾値)、推奨運用ルール(「このケースではこうせよ」形式のフローチャートまたは表)
2. `results/` : 全試行の生 CSV + 集計 CSV + グラフ(スループット比較、シャードサイズ掃引カーブ、多重負荷劣化、損益分岐)
3. `scripts/`: fio ジョブファイル、データセット生成、DataLoader 計測、Slurm バッチテンプレート(パターン A/B/C)、キャッシュ管理スクリプト。README に再実行手順を記載

## 7. 禁止・注意事項

- 本番データ・他ユーザのジョブに影響を与えないこと。多重負荷試験は事前に人間へ実施タイミングを確認する
- scratch / キャッシュ領域に置いたベンチデータは各フェーズ終了時に削除し、削除を確認する
- drop_caches 不可のままキャッシュが効いた状態の数値をレポートに載せないこと(載せる場合は warm 計測と明示)
- 結果が事前仮説と食い違った場合は、副計測ログ(iostat 等)で原因を特定してから次に進む。原因不明のまま数値だけ採用しない
- 途中でノード構成・パス等の前提が指示書と異なると判明した場合は、作業を止めて人間に確認する