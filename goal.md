# Slurm クラスタ IO ベンチマークライブラリ 開発指示書

## 0. ミッション

異種構成の計算ノードにまたがって**統一的な方法で IO・データ供給性能を計測できるベンチマークライブラリ**を開発せよ。実際の計測キャンペーンはライブラリ完成後の適用フェーズであり、本指示書の主目的はライブラリそのものの設計・実装・検証である。

ライブラリが最終的に答えを出すべき意思決定は以下の 3 つ。ライブラリの機能要件はすべてここから逆算する。

1. **供給経路の判断**: どの条件で HDD / NFS から直接プロセスにデータ供給してよく、どの条件で SSD scratch へのステージングを挟むべきか(損益分岐条件の定式化)
2. **フォーマットの判断**: 生ファイル / WebDataset / HDF5 / Zarr のうち、どのケースでどれを推奨するか。シャード・チャンクの最適サイズ帯の特定を含む
3. **ステージング運用の判断**: ジョブ内ステージング / 依存ジョブによる事前転送 / ノードローカル共有キャッシュのどれで運用すべきか

## 1. 環境情報

- Slurm クラスタ。Parent(ヘッド)ノード 1 台 + 計算ノード複数台
- 計算ノードは 3 構成:
  - **構成 X**: Parent と NFS 共有のファイルサーバをマウント + ノードローカル NVMe SSD scratch
  - **構成 Y**: ノードローカル HDD 記憶領域 + SSD scratch
  - **構成 Z**: GB10、ユニファイドメモリ(CPU/GPU 共有メモリ)
- マスターデータの置き場所は NFS ファイルサーバ

ノード構成が異種であることが本ライブラリの存在理由である。**「どのノードで実行しても、同じコマンド・同じ設定ファイルで、比較可能な結果が出る」**ことを最重要の設計原則とする。

### 実装前に人間に確認すること(不明なら質問せよ。推測で進めない)

- [ ] 各構成のノード名 / パーティション名 / constraint
- [ ] 各ストレージの実マウントパス(NFS、HDD、SSD scratch、`$SLURM_TMPDIR` の有無)と空き容量
- [ ] `drop_caches` の実行手段(root / sudo / prolog / 不可)
- [ ] `--exclusive` の可否、計測に使ってよい時間帯
- [ ] Python バージョン、fio / PyTorch / webdataset / h5py / zarr(v3 対応)/ tensorstore の導入可否、venv・コンテナの利用方針
- [ ] 実ワークロードの想定: 画像 1 枚のサイズ・形式、データセット総量、モデル種別、典型エポック数、同時ジョブ数

## 2. ライブラリ設計要件

### 2.1 アーキテクチャ(モジュール構成)

```
iobench/
├── probe/        # 環境自動検出
├── storage/      # ストレージ素性計測(fio ラッパ)
├── datagen/      # 合成データセット生成(4 フォーマット)
├── loaderbench/  # DataLoader 実効スループット計測
├── staging/      # 転送・ステージング計測 + 運用パターン A/B/C 実装
├── sidecar/      # 副計測(iostat / dstat / nvidia-smi dmon)の起動・収集
├── cache/        # ページキャッシュ制御(drop_caches / 代替戦略)
├── results/      # 結果スキーマ、CSV/JSON 書き出し、集計
├── slurm/        # sbatch テンプレート生成、ジョブ投入、依存関係管理
└── cli.py        # 統一 CLI エントリポイント
```

言語は Python。CLI は `iobench <subcommand>` 形式(例: `iobench probe`, `iobench storage --target /scratch`, `iobench loader --config exp.yaml`)。
ライブラリ管理にはuvを用い、pyproject.tomlで依存関係を明示する。Python 以外の言語で書く場合は、Python から呼び出せる CLI を提供すること。
Slurm環境での実行を前提とするため、sbatchで投入可能な.shスクリプトを作成すること。実験回の記録を残せるようにすること。

### 2.2 環境自動検出(probe モジュール)— 異種ノード対応の要

実行ノード上で以下を自動収集し、結果レコードに必ず埋め込む:

- ホスト名、Slurm 変数(job id, partition, `$SLURM_TMPDIR`)
- マウント一覧と各パスのファイルシステム種別(NFS / ext4 / xfs / tmpfs)、ブロックデバイス種別(rotational フラグで HDD/SSD 判定、NVMe 判定)
- CPU コア数、RAM 容量、GPU 有無・型番、ユニファイドメモリか否か
- 設定ファイルに書かれた論理ストレージ名(`nfs`, `hdd`, `ssd_scratch`, `tmpfs`)→ 実パスの解決。ノードごとのパス差異はここで吸収する

probe の結果から「このノードは構成 X/Y/Z のどれか」を自動分類できること。分類できない場合はエラーで止める(黙って計測しない)。

### 2.3 計測の統一ルール(ライブラリの組み込み機能として実装)

以下は運用者のモラルに頼らず、**ライブラリが強制する**:

1. **ページキャッシュ制御**: 各試行前に drop_caches を実行。権限がない場合は (a) データセットサイズ ≥ RAM×2 の検証、または (b) warm 計測であることを結果レコードに `cache_state: warm` として強制記録。cold と warm が混ざった比較を集計段でエラーにする
2. **反復と統計**: 1 条件 N 回(既定 3)実行し、中央値・min・max を自動算出。生の各試行も保存
3. **副計測の自動並走**: すべての計測で sidecar(iostat -x 1 / vmstat / dmon)を自動起動・自動回収し、試行 ID に紐付けて保存
4. **結果スキーマの固定**: 1 試行 = 1 レコード(JSON Lines + 集計 CSV)。必須フィールド: 試行 ID、タイムスタンプ、probe 結果全部、条件(フォーマット、シャードサイズ、workers、シャッフル方式、デコード有無、ストレージ論理名)、指標(下表)、cache_state、反復番号、ライブラリのバージョン(git hash)
5. **設定ファイル駆動**: 実験マトリクスは YAML で宣言し、CLI が展開して逐次 or Slurm ジョブとして実行。コマンドライン即席実行も可能だが、その場合も使った条件が結果に完全記録される

### 2.4 計測指標(loaderbench / storage / staging が出力すべき値)

| 指標 | 定義 |
|---|---|
| 実効スループット | デコード込みの samples/s と MB/s(デコードなし系列も選択可) |
| GPU 待ち時間 | データ待ちによる GPU アイドル(PyTorch Profiler / dmon 使用率) |
| ステージング時間 T_stage | ソース→SSD 転送所要時間(ツール・並列度別) |
| first-batch latency | 開始→最初のバッチ取得まで |
| 多重負荷劣化率 | N ジョブ同時実行時の単独比 |
| メタデータ性能 | open/stat ops/s(NFS 向け) |

### 2.5 機能要件(サブコマンド別)

- **`iobench storage`**: fio ラッパ。シーケンシャル/ランダム × ブロックサイズ {4K, 128K, 1M, 16M} × iodepth {1, 32} × numjobs {1, 8} を定義済みプリセットとして持つ。NFS 用の小ファイル大量 stat/read プリセットも同梱
- **`iobench datagen`**: 合成画像データセット生成。同一内容を 4 形態で出力:
  1. 生ファイル(ディレクトリあたりファイル数を変数化: 1k / 10k / 100k)
  2. WebDataset tar シャード(シャードサイズ指定可: 10MB〜5GB)
  3. HDF5(チャンクサイズ指定可: 64KB〜16MB)
  4. Zarr v3 sharded(同チャンク掃引)+ 参考用 Zarr v2
  シード固定で再現可能にし、生成物のファイル数・総サイズ・生成時間も記録する
- **`iobench loader`**: PyTorch DataLoader で 1 エポック読み切り計測。変数: フォーマット、ストレージ、num_workers {0,4,8,16}、シャッフル {なし / シャード内 / 完全ランダム}、デコード {あり / なし}
- **`iobench staging`**: 転送計測({cp, rsync, tar パイプ, シャード単位並列 rsync} × bwlimit 有無)と、損益分岐エポック数 `E: T_stage < E × (T_epoch_direct − T_epoch_SSD)` の自動算出
- **`iobench multi`**: 多重負荷試験のオーケストレーション。N 本の同一ジョブを同時投入し、各ジョブの結果を突合して劣化率を集計
- **`iobench slurm`**: sbatch テンプレート生成と投入。ステージング運用パターンを実装:
  - **パターン A**: ジョブ内ステージング
  - **パターン B**: CPU パーティション転送ジョブ + `--dependency=afterok` + `-w` 固定の学習ジョブ
  - **パターン C**: `/scratch/cache/<dataset_hash>/` 共有キャッシュ。flock 排他、存在チェックによるスキップ、LRU 容量管理スクリプトを含む
- **`iobench report`**: 結果 JSONL/CSV から集計表とグラフ(スループット比較、シャードサイズ掃引カーブ、多重負荷劣化、損益分岐表)を生成

### 2.6 非機能要件

- 各モジュールに単体テスト。計測ロジックは小さなダミーデータで CI 的に検証可能にする
- ベンチデータ・キャッシュの**後始末コマンド**(`iobench clean`)を用意し、消し忘れを防ぐ
- ドキュメント: README(インストール、クイックスタート、ノード追加手順)、設定ファイルのスキーマ説明
- Git 管理。結果レコードに git hash を埋め、どの版のライブラリで測った数値かを常に追跡可能にする

## 3. 開発フェーズ

**各フェーズ末尾のレビューポイントで人間の確認を得てから次へ進むこと。**

### フェーズ 1: 設計

- モジュール構成、結果スキーマ、設定 YAML スキーマ、CLI インタフェースの設計文書を作成
- **→ レビュー: スキーマと CLI 設計の承認を得る**(ここが後から変えにくい部分)

### フェーズ 2: コア実装

- probe / cache / sidecar / results を実装。この 4 つが計測の信頼性を担保する土台
- storage(fio ラッパ)と datagen を実装
- 単体テスト整備

### フェーズ 3: loaderbench + 単一ノード検証

- loaderbench 実装
- 構成 X の 1 ノードで小規模データ(〜10GB)によるスモークラン。結果スキーマ通りに記録されること、cold/warm 制御が効いていること、副計測が紐付くことを確認
- **→ レビュー: スモークラン結果の提示**

### フェーズ 4: staging / multi / slurm 実装 + 異種ノード検証

- 残りのサブコマンドを実装
- 構成 X / Y / Z の各 1 ノードで同一の設定ファイルを実行し、probe による構成自動分類とパス解決が正しく機能することを実証(**異種ノード統一計測の受け入れ試験**)
- パターン A/B/C のジョブテンプレートが実際に流れることを確認
- **→ レビュー: 異種ノード検証結果の提示**

### フェーズ 5: 計測キャンペーン(ライブラリの適用)

ライブラリを用いて本計測を実施する。順序:

1. **ストレージ素性**: 全ストレージで `iobench storage` 一式
2. **本命マトリクス**: 本番規模データセット(〜100GB)を生成し、フォーマット × ストレージ × アクセスパターンを計測。まず {WebDataset 500MB, 生ファイル} × {NFS, HDD, SSD} × {workers 8, シャードシャッフル, デコード込み} をスモークとして先行し、傾向を見て掃引範囲を絞ってよい(絞った理由は記録)
3. **→ 中間報告: ここまでの結果と続行方針の確認**
4. **損益分岐**: `iobench staging` で転送計測と E 閾値表の作成
5. **多重負荷**: `iobench multi` で 2/4/8 本同時試験(実施タイミングは事前に人間へ確認)
6. **運用パターン比較**: A/B/C の実測比較。パターン C は 2 本目ジョブでステージングスキップが効くことの実証を含む
7. (余力があれば)チェックポイント書き込み: NFS 直書き vs ローカル SSD 書き→非同期退避

### フェーズ 6: レポート

`iobench report` の出力を基に `report.md` を作成: 意思決定 3 点への回答、下記仮説の判定表、損益分岐表(構成 × フォーマット × エポック閾値)、「このケースではこうせよ」形式の推奨運用ルール。

## 4. 検証すべき仮説(計測キャンペーンの結果はこれへの判定として整理)

- H1: HDD 直読みは「シーケンシャル形式 + シャードシャッフルのみ + 単独ジョブ + 要求スループット ≤ HDD 逐次読み性能」で成立する
- H2: 完全ランダムアクセスは HDD / NFS 上では実用不可レベルに劣化する
- H3: WebDataset のシャード最適帯は 100MB–1GB。HDD 直読みでは大きめが有利
- H4: 生ファイルは SSD 上では十分速いが NFS 上では最悪
- H5: NFS の多重負荷劣化は直読み方式で顕著、ステージング方式はほぼ免疫
- H6: GB10 ではデータがメモリに収まる場合フォーマット差がほぼ消え、CPU デコードが支配的になる
- H7: 同一データセットを繰り返し使う運用ではパターン C が総所要時間で最良

各仮説に「支持 / 棄却 / 条件付き支持」と根拠データ(試行 ID で追跡可能に)を明記。

## 5. 最終成果物

1. **ライブラリ本体**(リポジトリ): 上記モジュール一式、単体テスト、README、設定サンプル、sbatch テンプレート
2. **計測結果** `results/`: 全試行 JSONL、集計 CSV、グラフ
3. **`report.md`**: 意思決定 3 点への回答と推奨運用ルール

## 6. 禁止・注意事項

- 本番データ・他ユーザのジョブに影響を与えない。多重負荷試験は事前に実施タイミングを人間へ確認
- ベンチデータは `iobench clean` で削除し、削除を確認する
- cold/warm が混在した比較を報告しない(ライブラリでの強制に加え、レポート段でも確認)
- 結果が仮説と食い違う場合は sidecar ログで原因を特定してから進む。原因不明の数値を採用しない
- ノード構成・パス等の前提が本指示書と異なると判明したら、作業を止めて人間に確認する
- ライブラリ開発中の「動作確認用ミニ計測」の数値を本計測の結果に混入させない(スキーマの `purpose: dev|campaign` フィールドで区別する)